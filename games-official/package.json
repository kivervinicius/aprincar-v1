{
  "name": "aprincar-games-official",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "prepare": "node scripts/install-hooks.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build-games.mjs",
    "validate": "node scripts/validate.mjs",
    "registry": "node scripts/build-registry.mjs",
    "check": "npm run test && npm run build && npm run syntax && npm run validate && npm run registry",
    "syntax": "node --check src/runtime/sdk-bridge.js && node --check src/runtime/brand.js && node --check src/common/*.js && node --check src/games/*.js && node scripts/check-generated.mjs"
  },
  "description": "Official procedural Phaser and Three.js game extensions for Aprincar"
}
