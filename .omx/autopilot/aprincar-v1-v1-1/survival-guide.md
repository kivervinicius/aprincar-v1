# Survival guide

V1.1 está em `main` e publicada em `https://kivervinicius.github.io/aprincar-v1/`. O último commit funcional é `8cef819`; mudanças locais restantes são apenas screenshots de validação não commitadas. Para retomar: conferir `git status`, `gh run list --workflow pages.yml` e o registry público.
