import*as THREE from'three';import{aprincar}from'./aprincar-sdk';const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(70,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);document.querySelector('#app')!.append(renderer.domElement);const cube=new THREE.Mesh(new THREE.BoxGeometry(),new THREE.MeshNormalMaterial());scene.add(cube);camera.position.z=3;renderer.domElement.onclick=async()=>{await aprincar.evidence.submit({skillId:'math.geometry.solids',result:'observed',independent:true,assistance:'none',difficulty:.3,confidence:.6})};(async()=>{await aprincar.session.start();function loop(){cube.rotation.x+=.01;cube.rotation.y+=.013;renderer.render(scene,camera);requestAnimationFrame(loop)}loop()})();