# Three.js game template

Use this template for lightweight 3D experiences. The build bundles Three.js into a single self-contained artifact.

Verify WebGL fallback, resize, pointer/touch interaction, camera behavior and offline loading. Declare only the permissions required by the experience and emit Evidence through the SDK. Run `npm run check` before submission.

