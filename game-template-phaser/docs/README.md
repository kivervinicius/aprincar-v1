# Phaser game template

Use this template for animated 2D games. Phaser is bundled into the extension build so the artifact does not require a CDN.

Implement responsive canvas interaction, visible correct/incorrect feedback, deterministic challenge handling and SDK Evidence events. Validate the generated single-file artifact with `npm run check`.

