type Pending={resolve:(v:any)=>void,reject:(e:any)=>void};export class AprincarGame{private port:MessagePort|null=null;private pending=new Map<string,Pending>();private readyPromise:Promise<void>;private readyResolve!:()=>void;constructor(){this.readyPromise=new Promise(r=>this.readyResolve=r);addEventListener('message',e=>{if(e.data?.type!=='APRINCAR_CONNECT'||e.data?.protocolVersion!==1||!e.ports?.[0])return;this.port=e.ports[0];this.port.onmessage=ev=>{const m=ev.data,p=this.pending.get(m.requestId);if(!p)return;this.pending.delete(m.requestId);m.ok?p.resolve(m.payload):p.reject(new Error(m.error))};this.port.start();this.readyResolve();this.port.postMessage({type:'game.ready',payload:{protocolVersion:1}})})}async request(type:string,payload:unknown={}){await this.readyPromise;const requestId=crypto.randomUUID();return new Promise((resolve,reject)=>{this.pending.set(requestId,{resolve,reject});this.port!.postMessage({type,requestId,payload})})}session={start:(p={})=>this.request('session.start',p),complete:(p={})=>this.request('session.complete',p)};evidence={submit:(p:unknown)=>this.request('evidence.submit',p)};rewards={request:(p:unknown)=>this.request('reward.request',p)};storage={get:(key:string)=>this.request('storage.get',{key}),set:(key:string,value:unknown)=>this.request('storage.set',{key,value})};capability={request:(name:string,payload?:unknown)=>this.request('capability.request',{name,payload})}}
export const aprincar=new AprincarGame();