import React from'react';import{createRoot}from'react-dom/client';import{aprincar}from'./aprincar-sdk';import'./style.css';function App(){const[done,setDone]=React.useState(false);React.useEffect(()=>{aprincar.session.start()},[]);return <main><h1>Meu Jogo React</h1><button onClick={async()=>{await aprincar.evidence.submit({skillId:'logic.patterns.ab',result:'success',independent:true,assistance:'none',difficulty:.4,confidence:.9});setDone(true)}}>Completei o desafio</button>{done&&<p>Evidência enviada ✓</p>}</main>}createRoot(document.getElementById('app')!).render(<App/>);