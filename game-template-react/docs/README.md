# React game template

Use this template when the game benefits from React component state and rendering.

The production contract remains a self-contained `game.html` plus manifest and integrity metadata. Keep educational state inside the game, emit Evidence through the SDK, declare only required permissions and validate the generated `package/` output with `npm run check`.

