# Contributing

Use Conventional Commits. Architecture changes require an ADR/RFC. Extension protocol changes require tests proving backward compatibility or an explicit protocol version bump. Skill changes require pedagogical review. Run `npm run check` before opening a PR.
